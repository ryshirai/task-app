# Database Configuration
# For local dev: postgresql://postgres:postgres@localhost:5432/task_management
# For Supabase (Transaction Mode): postgresql://postgres.[your-project]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/task_management
DB_MAX_CONNECTIONS=1
RUN_MIGRATIONS=false
DB_IDLE_TIMEOUT_SECONDS=30
DB_MAX_LIFETIME_SECONDS=1800

# Authentication
JWT_SECRET=your_super_secret_jwt_key_here

# Domain & Application
# Required for generating reset/invite links and default emails.
DOMAIN=rysh.dev
# Optional: Override if different from https://${DOMAIN}
# FRONTEND_URL=http://localhost:5173

# Email Configuration
# Options: stdout, ses
EMAIL_PROVIDER=stdout
# The address that will appear as the sender. Must be verified in SES if using provider=ses.
EMAIL_FROM_ADDRESS=no-reply@rysh.dev

# AWS Configuration (Required for SES/Lambda)
AWS_REGION=ap-northeast-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Server Configuration (Local only)
PORT=3000
